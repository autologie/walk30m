box: node
build:
  steps:
    - script:
      name: install
      code: |
        yarn install
    - script:
      name: test
      code: |
        yarn test
deploy:
  steps:
    - script:
      name: deploy
      code: |
        echo "TODO: deploy using netlify"
